# Skill 修复验收 · 2026-09-21

范围：14 个发布 Skill，以及本项目内 4 个辅助/实验 Skill。本记录对应提交前的
验收快照；全局部署和第三方缓存未改动。

## 已处理

| 范围       | 结果                                                                                |
| ---------- | ----------------------------------------------------------------------------------- |
| 触发边界   | 描述按任务意图区分 PR、Release、Issue、指标、仓库评估、文档和 Skill 设计            |
| 事实与执行 | 删除虚构参数、默认值、背景和性能结论；区分证据、方案与实际验证                      |
| PR         | 去除无效权限字段及重复审批；沿用明确授权，失败不冒充成功                            |
| OpenRank   | 独立解析 helper 支持嵌套统计、准确周期、零值及不同失败状态                          |
| 许可证     | 按精确版本和组合/分发场景给条件结论，纠正 LGPL、AGPL 与兼容性矩阵                   |
| 分发       | Marketplace 补齐 14 项，逐项指定自身 Skill，README 与网站说明同步                   |
| 维护入口   | 三个项目根共 42 个相对链接；完整备份和辅助补丁哈希可追溯                            |
| 辅助 Skill | hue 按模式读取；impeccable 局部任务免初始化；本地 creator 改名；Linear 支持离线/CJK |

## 验收证据

| 检查                                    | 实际结果                                                                                                      |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `mise run check`                        | 14 个 Skill、85 个用例静态检查无错误；26 项自动测试通过                                                       |
| `mise run skills:check-local`           | 42 个入口，0 个路径或来源问题                                                                                 |
| `mise run website:build`                | TypeScript + Vite 构建通过；现有 Browserslist 数据陈旧提示不影响构建                                          |
| Claude Code 2.1.278 `plugin validate .` | 原生 marketplace 校验通过，临时 npm exec，无全局安装                                                          |
| OpenDigger 真实请求                     | X-lab2017/open-digger：OpenRank 最新月 2026-08 = 3.23；issue_response_time 最新月 2026-02 avg = 2 天，均为 ok |
| 四项辅助补丁重放                        | 对完整备份应用补丁后，所有改动文件 SHA-256 与当前副本一致                                                     |
| 辅助执行检查                            | scaffolder 初始化和格式验证通过；impeccable 缺/有 PRODUCT 及保留本地补丁的更新提示通过                        |
| 文档                                    | Prettier、相对链接、Git whitespace 检查通过                                                                   |

OpenDigger 两个指标的最新月份不同，报告保留各自真实周期，没有向最新月补值。
单个样本可证明当前接口和解析路径能工作，不代表所有仓库与指标都可获取。

## 模型评估

核心回归集：56 个触发用例（28 正例、28 相邻任务反例）和 29 个行为用例。
执行者只接收请求、候选目录、Skill 和必要 fixture，未接收期待答案或评分标准。
行为答复由主审逐条对照 rubric 和实际工具记录审阅，保存观察文件哈希。

第一轮 **82/85**。失败为开发环境文档漏触发、旧 OpenRank 资源根缺引用，
以及材料不全的项目比较未明确后续统一时间窗口。修复后第二轮 **85/85**：
56 个路由选择及 29 个行为答复均通过本组检查。

另对四个辅助 Skill 使用包含 18 项的合并候选目录评估：8 个路由请求、4 个
仅解释的行为请求，逐项复核通过。已有 Git Skill 的剩余验证正确路由到
skill-architect，未为此调用脚手架或强制生成归档。

这不是生产触发准确率。第二轮沿用同一批评估者的上下文；未做不加载 Skill 的
基线、全新上下文盲测、多模型或重复随机试验。宿主是 Codex desktop，模型继承
当前任务配置，工具未导出精确模型标识。fixture/只读评估未执行真实 PR 写入、
许可证替换、容器部署或第三方发布。

## 复核材料与边界

本机完整请求、观察、评审、资源快照、版本信息与源码哈希位于：

```text
~/.codex/visualizations/2026/09/21/01a0c3dc-8eb0-7613-816d-5a95a6e527d1/skill-repair-evidence/
```

`initial/score-summary.json` 保留第一轮结论；`final/score-summary.json` 是第二轮。
辅助结果在 `auxiliary/review-summary.json`。历史请求记录的是执行时 `/tmp` 快照
路径，归档保留原样；新一轮应使用 [评测指南](evaluation.md) 重新准备请求。
审查原文是修复前的历史报告，不代表当前状态。

原生校验与独立目录复制检查已完成；没有把插件安装到另一宿主执行完整加载
流程，也没有用新会话验证 Codex/Claude/Trae 的实际自动发现。现有会话可能仍缓存
旧描述，需重新加载或新建会话。备份、补丁应用与恢复见 [本地维护](local-skills.md)。
